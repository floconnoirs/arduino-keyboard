# printedBase — needs re-upload

This folder was empty when the repo was reorganized (the original
`printedBase` folder came through as a 0-byte file on upload).

**Action needed:** add the 3D-print files for the keypad's printed base here
(e.g. `.stl` / `.step` / slicer project files).
